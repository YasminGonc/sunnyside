import React from 'react';

import { Container, Title, ArrowIcon } from './styles';

const Banner: React.FC = () => {
    return (
        <Container>
            <Title>We are creatives</Title>
            <ArrowIcon />
        </Container>
    );
}

export default Banner;